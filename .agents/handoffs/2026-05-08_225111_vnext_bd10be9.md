Branch: vnext | HEAD: bd10be9 | 2026-05-08

## Handoff - Schema Consolidation (Migration 30)

**Repo:** C:\Users\scottm\repo\data-platform
**Session commits:** 050198e..bd10be9

## State
- Tests: baseline 1375 passed, 7 skipped (no test changes this session)
- ERD: 31 tables, 33 FKs, 333 columns (post migration 30)
- Ruff/mypy: no source changes this session

## Delivered this session

### Migration 29 (050198e)
- Linked 5 orphaned profiling tables into the FK graph
- profiling.ColumnStats / KeyCandidates / Relationships: added ObjectName FK → control.ObjectConfig
- profiling.PiiCandidates + catalog.TableContracts: enforced FK → control.ObjectConfig

### Migration 30 (7d8dcc8)
- Rename control.UnpackSpec → control.JsonFlattenConfig
- Merged control.IngestionSource into control.SourceConfig (SourceID becomes PK, IngestionSource dropped)
- Inverted SourceConfig↔ObjectConfig relationship: ObjectConfig.SourceConfigID FK → SourceConfig
- WatermarkState: replaced composite PK with WatermarkID INT IDENTITY surrogate; UNIQUE(SourceID,ObjectName) preserved
- profiling.ObjectProfile: replaced composite PK with ObjectProfileID INT IDENTITY; UNIQUE(ObjectName,RunID) preserved
- ColumnStats / KeyCandidates / Relationships: added ObjectProfileID FK → ObjectProfile

### ERD generator fixes (scripts/generate_control_erd.py)
- Added _ALTER_ADD_PK_RE + _alter_add_pk() — tracks ALTER TABLE ADD CONSTRAINT PRIMARY KEY
- Fixed _ALTER_ADD_RE with negative lookahead (?!CONSTRAINT\b) — eliminates pseudo-column artefacts (removed 12 phantom columns)

### ERD regenerated (eda8d0d + dbdiagram committed)
### Diary entries committed (bd10be9)

## Next task
Continue Sprint 18 — OneLake RBAC + Livy CI smoke (GAP-21)
- ADR-057 (OneLake RBAC strategy) not yet written
- scripts/20-configure-onelake-access-roles.py not yet created
- scripts/smoke_livy.py + tests not yet created
Run /run-sprint GAP-21 to begin TDD cycles.
