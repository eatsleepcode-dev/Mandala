Branch: vnext | HEAD: a1105d7 | 2026-05-06

## Handoff — Sprint 13 CLOSED, Sprint 14 next

**Repo:** C:\Users\scottm\repo\data-platform
**Last sprint:** Sprint 13 — Source Onboarding Wizard — COMPLETE
**Commits this session:** 25033d3..a1105d7

## Baseline
- Tests: ✅ test_nb_create_dimdate.py — 29 passed (all unit tests); 5 new
  dual-route [mock] variants pass. Full suite collection still has pre-existing
  trio/attrs/PackagePath crash in local env — not introduced this session.
  Fix: `pip install --upgrade attrs trio anyio` (or use CI).
- mypy: ✅ 0 errors (pii_scanner.py, nb_bronze_profiling.py confirmed clean)
- HIGH tech debt: 0

## Delivered this session
- feat(tests): dual-route Spark fixtures (mock+real) in tests/conftest.py (a1105d7)
  - `real_spark` — session-scoped, auto-skips when Java absent; injects
    portable JAVA_HOME/bin into PATH if set
  - `dual_spark` — parametrized mock|real; each consumer test runs twice
  - TestDimDateDfDual — 5 dual-route tests in test_nb_create_dimdate.py
    (row count, schema columns, DateKey int type, IsWeekend 0/1)
- docs: CONTRIBUTING.md two-tier test architecture + portable JDK17 setup
  script (no admin / Admin By Request workaround)
- docs: developer-guide.md prerequisites updated for PySpark 4.x / Java 17
- Java 17.0.19 portable installed at C:\Users\scottm\tools\jdk17\ (no admin)
  - JAVA_HOME, HADOOP_HOME, PATH set at User scope
  - winutils.exe + hadoop.dll (Hadoop 3.3.6) at C:\Users\scottm\tools\hadoop\bin\

## Previous session (400081f) — Sprint 13 COMPLETE
- nb_jdbc_introspect.py — PK inference, proposed ObjectConfig rows (IsActive=False)
- nb_source_wizard.py — 7-step guided source registration (JDBC/REST/File/Dataverse)
- nb_maintenance_objectconfig.py — CleansingRules tab (4 rule types + preview)
- nb_bootstrap.py — CleansingRules.RuleType migration guard
- TD-038 resolved; ADR-056 accepted

## Open items
- ⚠️ Local full-suite collection broken (trio/attrs env) — pre-existing, not a CI blocker
- dual_spark [real] variants need JAVA_HOME in process to run — works in a new
  shell after VS Code restart (env vars persisted at User scope)
- test_output.txt untracked, safe to delete

## Next task
Sprint 14 — Observability
Prerequisite: Sprint 13 COMPLETE ✅
Run /build-sprint for Sprint 14 to generate the task card.

Key deliverables:
- ControlLog structured logging (DurationSeconds, SourceRowCount, SinkRowCount)
- nb_landing_to_bronze: add log_event call with row counts
- PipelineMonitorPlugin — ControlLog backend; WI token path
- DqRulesPlugin — History tab with pass-rate trend
- LogViewerPlugin — ControlLog Errors tab
